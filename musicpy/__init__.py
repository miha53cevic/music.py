# Allows the ability to run import musicpy and then use modules
# Python 3 must use .module for relative path to the module inside the folder
# from .music import AudioPlayer, main()

# Left empty just tells setuptools that everything inside musicpy are modules
# But if empty you can't import any of them